Chat room that works on shared folders

you need to put this on a pc that has a network drive that others can access.
The user will need read, write and execute permissions
each user can run the run.bat file and can type in messages and othes with the program can see messages.
it also it a notification sound when there is a new message

To-do
add an auto update
have the script to nit pause when doing a notification sound
only update the window if there is a new message
